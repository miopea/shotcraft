---
"shotcraft": patch
---

Mark the planned `dev` and `list` subcommands as "(coming soon)" in `shotcraft --help` so first-run users aren't misled, add a package README (npm landing page), and point the package `homepage` at the GitHub repo.
